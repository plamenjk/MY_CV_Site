@{
    ViewData["Title"] = "Контакти";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="glass-card p-4">
                <h2 class="mb-3">Контакти</h2>
                <p class="text-muted">
                    Ако имаш идея за проект, въпрос или просто искаш да си поговорим за коли и технологии –
                    пиши ми чрез формата по-долу.
                </p>
                <form method="post" class="mt-3">
                    <div class="mb-3">
                        <label class="form-label">Име</label>
                        <input type="text" name="name" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">E-mail</label>
                        <input type="email" name="email" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Съобщение</label>
                        <textarea name="messageText" class="form-control" rows="4" required></textarea>
                    </div>
                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger">@ViewBag.Error</div>
                    }
                    @if (ViewBag.Success != null)
                    {
                        <div class="alert alert-success">@ViewBag.Success</div>
                    }
                    <button type="submit" class="btn btn-primary btn-neo w-100">Изпрати</button>
                </form>
            </div>
        </div>
    </div>
</div>
